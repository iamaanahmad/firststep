[package]
name = "firststep_sponsorship"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = { version = "0.29", features = ["init-if-needed"] }
anchor-spl = "0.29"

[lib]
crate-type = ["cdylib", "lib"]

[profile.release]
overflow-checks = true
